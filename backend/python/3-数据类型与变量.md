# 第3章：数据类型与变量

> **学习时长**: 6-8小时  
> **难度**: ⭐⭐⭐  
> **前置知识**: 第1-2章

## 本章目标

学完本章后,你将能够:

- ✅ 掌握Python的基本数据类型(数字、字符串)
- ✅ 熟练使用容器类型(列表、元组、字典、集合)
- ✅ 理解可变与不可变类型的区别
- ✅ 进行类型转换与类型判断
- ✅ 使用各种数据类型的内置方法

---

## 3.1 数字类型

### 3.1.1 整数 (int)

```python
# 整数
x = 10
y = -5
z = 0

# 大整数(Python 3无限制)
big_num = 123456789012345678901234567890

# 不同进制
binary = 0b1010      # 二进制 = 10
octal = 0o12         # 八进制 = 10
hexadecimal = 0xa    # 十六进制 = 10

print(binary, octal, hexadecimal)  # 10 10 10
```

**整数运算**:

```python
a = 10
b = 3

print(a + b)   # 13  加法
print(a - b)   # 7   减法
print(a * b)   # 30  乘法
print(a / b)   # 3.333... 除法
print(a // b)  # 3   整除
print(a % b)   # 1   取余
print(a ** b)  # 1000 幂运算

# 内置函数
print(abs(-10))      # 10  绝对值
print(pow(2, 3))     # 8   幂运算
print(divmod(10, 3)) # (3, 1) 返回商和余数
```

### 3.1.2 浮点数 (float)

```python
# 浮点数
x = 3.14
y = -2.5
z = 0.0

# 科学计数法
large = 1.5e6   # 1500000.0
small = 1.5e-6  # 0.0000015

# 浮点数精度问题
print(0.1 + 0.2)  # 0.30000000000000004

# 解决方案: 使用round()
result = round(0.1 + 0.2, 2)
print(result)  # 0.3

# 或使用decimal模块
from decimal import Decimal
x = Decimal('0.1')
y = Decimal('0.2')
print(x + y)  # 0.3
```

**浮点数函数**:

```python
import math

print(math.ceil(3.2))   # 4   向上取整
print(math.floor(3.8))  # 3   向下取整
print(round(3.5))       # 4   四舍五入
print(math.sqrt(16))    # 4.0 平方根
print(math.pi)          # 3.141592653589793
```

### 3.1.3 复数 (complex)

```python
# 复数
z = 3 + 4j
print(type(z))  # <class 'complex'>

# 实部和虚部
print(z.real)   # 3.0
print(z.imag)   # 4.0

# 复数运算
z1 = 2 + 3j
z2 = 1 - 1j
print(z1 + z2)  # (3+2j)
print(z1 * z2)  # (5+1j)

# 共轭复数
print(z.conjugate())  # (3-4j)
```

---

## 3.2 字符串 (str)

### 3.2.1 字符串创建

```python
# 单引号
s1 = 'Hello'

# 双引号
s2 = "World"

# 三引号(多行字符串)
s3 = """这是
多行
字符串"""

s4 = '''也可以
使用单引号'''

# 转义字符
s5 = "He said \"Hello\""
s6 = 'It\'s a book'
s7 = "First line\nSecond line"  # 换行
s8 = "Tab\tseparated"           # 制表符

# 原始字符串(忽略转义)
path = r"C:\Users\name\Documents"  # Windows路径
```

### 3.2.2 字符串索引与切片

```python
s = "Python"

# 索引(从0开始)
print(s[0])   # 'P'
print(s[1])   # 'y'
print(s[-1])  # 'n' (最后一个)
print(s[-2])  # 'o' (倒数第二个)

# 切片 [start:end:step]
print(s[0:3])   # 'Pyt' (索引0到2)
print(s[2:])    # 'thon' (从索引2到结尾)
print(s[:4])    # 'Pyth' (从开始到索引3)
print(s[::2])   # 'Pto' (步长为2)
print(s[::-1])  # 'nohtyP' (反转)

# 字符串长度
print(len(s))   # 6
```

### 3.2.3 字符串方法

**大小写转换**:

```python
s = "Hello World"

print(s.upper())       # "HELLO WORLD"
print(s.lower())       # "hello world"
print(s.capitalize())  # "Hello world"
print(s.title())       # "Hello World"
print(s.swapcase())    # "hELLO wORLD"
```

**查找与替换**:

```python
s = "Hello World Hello"

# 查找
print(s.find("World"))    # 6 (返回索引)
print(s.find("Python"))   # -1 (未找到)
print(s.index("World"))   # 6 (未找到会报错)
print(s.count("Hello"))   # 2 (出现次数)

# 替换
print(s.replace("Hello", "Hi"))  # "Hi World Hi"
```

**分割与连接**:

```python
# 分割
s = "apple,banana,orange"
fruits = s.split(",")
print(fruits)  # ['apple', 'banana', 'orange']

s = "Hello World"
words = s.split()  # 默认按空格分割
print(words)  # ['Hello', 'World']

# 连接
fruits = ['apple', 'banana', 'orange']
s = ", ".join(fruits)
print(s)  # "apple, banana, orange"

separator = "-"
s = separator.join(['2024', '01', '15'])
print(s)  # "2024-01-15"
```

**去除空白**:

```python
s = "  Hello World  "

print(s.strip())   # "Hello World" (两端)
print(s.lstrip())  # "Hello World  " (左侧)
print(s.rstrip())  # "  Hello World" (右侧)
```

**判断方法**:

```python
s1 = "Hello"
s2 = "12345"
s3 = "Hello123"

print(s1.isalpha())   # True (全字母)
print(s2.isdigit())   # True (全数字)
print(s3.isalnum())   # True (字母或数字)
print(s1.islower())   # False
print(s1.isupper())   # False
print("  ".isspace()) # True (全空白)
```

**字符串格式化**:

```python
name = "Alice"
age = 25
score = 95.5

# f-string (推荐)
s = f"姓名:{name}, 年龄:{age}, 分数:{score:.1f}"
print(s)

# format()
s = "姓名:{}, 年龄:{}, 分数:{:.1f}".format(name, age, score)
print(s)

# % 格式化
s = "姓名:%s, 年龄:%d, 分数:%.1f" % (name, age, score)
print(s)
```

---

## 3.3 列表 (list)

### 3.3.1 列表创建

```python
# 空列表
empty_list = []
empty_list = list()

# 初始化列表
numbers = [1, 2, 3, 4, 5]
fruits = ["apple", "banana", "orange"]
mixed = [1, "hello", 3.14, True]

# 列表推导式
squares = [x**2 for x in range(1, 6)]
print(squares)  # [1, 4, 9, 16, 25]

# 嵌套列表(二维)
matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
]
```

### 3.3.2 访问元素

```python
fruits = ["apple", "banana", "orange", "grape"]

# 索引访问
print(fruits[0])   # "apple"
print(fruits[-1])  # "grape"

# 切片
print(fruits[1:3])  # ['banana', 'orange']
print(fruits[:2])   # ['apple', 'banana']
print(fruits[2:])   # ['orange', 'grape']
```

### 3.3.3 修改列表

```python
fruits = ["apple", "banana", "orange"]

# 修改元素
fruits[1] = "blueberry"
print(fruits)  # ['apple', 'blueberry', 'orange']

# 添加元素
fruits.append("grape")        # 末尾添加
print(fruits)

fruits.insert(1, "cherry")    # 指定位置插入
print(fruits)

# 删除元素
fruits.remove("apple")        # 删除指定值
print(fruits)

last = fruits.pop()           # 删除并返回最后一个
print(last, fruits)

del fruits[0]                 # 删除指定索引
print(fruits)

fruits.clear()                # 清空列表
print(fruits)
```

### 3.3.4 列表操作

```python
# 合并列表
list1 = [1, 2, 3]
list2 = [4, 5, 6]

# 方法1: +
result = list1 + list2
print(result)  # [1, 2, 3, 4, 5, 6]

# 方法2: extend()
list1.extend(list2)
print(list1)

# 重复列表
list3 = [1, 2] * 3
print(list3)  # [1, 2, 1, 2, 1, 2]

# 排序
numbers = [3, 1, 4, 1, 5, 9, 2]
numbers.sort()           # 原地排序
print(numbers)

sorted_nums = sorted([3, 1, 4])  # 返回新列表
print(sorted_nums)

# 反转
numbers.reverse()
print(numbers)

# 查找
fruits = ["apple", "banana", "orange"]
print(fruits.index("banana"))  # 1
print(fruits.count("apple"))   # 1
print("banana" in fruits)      # True
```

---

## 3.4 元组 (tuple)

### 3.4.1 元组特点

**元组是不可变的列表**:

```python
# 创建元组
empty_tuple = ()
single = (1,)  # 单元素元组需要逗号
numbers = (1, 2, 3, 4, 5)
mixed = (1, "hello", 3.14)

# 元组解包
x, y, z = (1, 2, 3)
print(x, y, z)  # 1 2 3

# 交换变量
a, b = 10, 20
a, b = b, a
print(a, b)  # 20 10
```

### 3.4.2 元组操作

```python
t = (1, 2, 3, 4, 5)

# 访问(同列表)
print(t[0])      # 1
print(t[2:4])    # (3, 4)

# 不能修改
# t[0] = 10  # TypeError

# 可以使用的方法
print(t.count(3))   # 1
print(t.index(4))   # 3
print(len(t))       # 5

# 元组拼接
t1 = (1, 2)
t2 = (3, 4)
t3 = t1 + t2
print(t3)  # (1, 2, 3, 4)
```

### 3.4.3 元组 vs 列表

| 特性 | 列表 | 元组 |
|------|------|------|
| 可变性 | 可变 | 不可变 |
| 符号 | `[]` | `()` |
| 性能 | 较慢 | 较快 |
| 用途 | 动态数据 | 固定数据 |

---

## 3.5 字典 (dict)

### 3.5.1 字典创建

```python
# 空字典
empty_dict = {}
empty_dict = dict()

# 创建字典
person = {
    "name": "Alice",
    "age": 25,
    "city": "Beijing"
}

# 使用dict()
person = dict(name="Bob", age=30, city="Shanghai")

# 字典推导式
squares = {x: x**2 for x in range(1, 6)}
print(squares)  # {1: 1, 2: 4, 3: 9, 4: 16, 5: 25}
```

### 3.5.2 访问字典

```python
person = {
    "name": "Alice",
    "age": 25,
    "city": "Beijing"
}

# 访问值
print(person["name"])  # "Alice"
print(person.get("age"))  # 25
print(person.get("gender", "未知"))  # "未知" (默认值)

# 获取所有键/值
print(person.keys())    # dict_keys(['name', 'age', 'city'])
print(person.values())  # dict_values(['Alice', 25, 'Beijing'])
print(person.items())   # dict_items([('name', 'Alice'), ...])

# 遍历字典
for key in person:
    print(f"{key}: {person[key]}")

for key, value in person.items():
    print(f"{key}: {value}")
```

### 3.5.3 修改字典

```python
person = {"name": "Alice", "age": 25}

# 添加/修改
person["city"] = "Beijing"  # 添加
person["age"] = 26          # 修改

# 更新多个
person.update({"age": 27, "gender": "女"})

# 删除
del person["city"]          # 删除键
age = person.pop("age")     # 删除并返回值
person.clear()              # 清空
```

### 3.5.4 字典方法

```python
person = {"name": "Alice", "age": 25, "city": "Beijing"}

# 检查键是否存在
print("name" in person)  # True
print("gender" in person)  # False

# setdefault - 如果键不存在则设置
person.setdefault("gender", "未知")
print(person)

# fromkeys - 创建字典
keys = ['a', 'b', 'c']
d = dict.fromkeys(keys, 0)
print(d)  # {'a': 0, 'b': 0, 'c': 0}

# 复制字典
person_copy = person.copy()
```

---

## 3.6 集合 (set)

### 3.6.1 集合特点

**集合**: 无序、不重复的元素集

```python
# 创建集合
empty_set = set()  # 注意: {} 是空字典
numbers = {1, 2, 3, 4, 5}
fruits = {"apple", "banana", "orange"}

# 自动去重
numbers = {1, 2, 2, 3, 3, 3}
print(numbers)  # {1, 2, 3}

# 从列表创建
numbers = set([1, 2, 2, 3, 3, 3])
print(numbers)  # {1, 2, 3}
```

### 3.6.2 集合操作

```python
s = {1, 2, 3}

# 添加元素
s.add(4)
print(s)  # {1, 2, 3, 4}

s.update([5, 6, 7])
print(s)  # {1, 2, 3, 4, 5, 6, 7}

# 删除元素
s.remove(7)     # 不存在会报错
s.discard(10)   # 不存在不报错
s.pop()         # 随机删除一个
s.clear()       # 清空
```

### 3.6.3 集合运算

```python
a = {1, 2, 3, 4, 5}
b = {4, 5, 6, 7, 8}

# 并集
print(a | b)  # {1, 2, 3, 4, 5, 6, 7, 8}
print(a.union(b))

# 交集
print(a & b)  # {4, 5}
print(a.intersection(b))

# 差集
print(a - b)  # {1, 2, 3}
print(a.difference(b))

# 对称差集
print(a ^ b)  # {1, 2, 3, 6, 7, 8}
print(a.symmetric_difference(b))

# 子集/超集
c = {1, 2}
print(c.issubset(a))    # True
print(a.issuperset(c))  # True
```

---

## 3.7 类型转换

```python
# 转整数
print(int("123"))      # 123
print(int(3.14))       # 3
print(int("0b1010", 2))  # 10 (二进制)

# 转浮点数
print(float("3.14"))   # 3.14
print(float(10))       # 10.0

# 转字符串
print(str(123))        # "123"
print(str(3.14))       # "3.14"
print(str([1, 2, 3]))  # "[1, 2, 3]"

# 转列表
print(list("hello"))   # ['h', 'e', 'l', 'l', 'o']
print(list((1, 2, 3))) # [1, 2, 3]
print(list({1, 2, 3})) # [1, 2, 3]

# 转元组
print(tuple([1, 2, 3]))  # (1, 2, 3)
print(tuple("hello"))    # ('h', 'e', 'l', 'l', 'o')

# 转集合
print(set([1, 2, 2, 3]))  # {1, 2, 3}
print(set("hello"))       # {'h', 'e', 'l', 'o'}

# 转字典
pairs = [('a', 1), ('b', 2)]
print(dict(pairs))  # {'a': 1, 'b': 2}
```

---

## 3.8 实验:通讯录管理程序

```python
"""
通讯录管理系统
功能: 添加、删除、查找、显示联系人
"""

# 使用字典存储通讯录
contacts = {}

def add_contact():
    """添加联系人"""
    name = input("姓名: ")
    phone = input("电话: ")
    email = input("邮箱: ")
    
    contacts[name] = {
        "phone": phone,
        "email": email
    }
    print(f"✓ 已添加联系人: {name}")

def delete_contact():
    """删除联系人"""
    name = input("请输入要删除的姓名: ")
    if name in contacts:
        del contacts[name]
        print(f"✓ 已删除联系人: {name}")
    else:
        print(f"✗ 未找到联系人: {name}")

def search_contact():
    """查找联系人"""
    name = input("请输入要查找的姓名: ")
    if name in contacts:
        info = contacts[name]
        print(f"\n姓名: {name}")
        print(f"电话: {info['phone']}")
        print(f"邮箱: {info['email']}")
    else:
        print(f"✗ 未找到联系人: {name}")

def show_all():
    """显示所有联系人"""
    if not contacts:
        print("通讯录为空")
        return
    
    print("\n" + "=" * 50)
    print(f"{'姓名':<10} {'电话':<15} {'邮箱':<20}")
    print("=" * 50)
    
    for name, info in contacts.items():
        print(f"{name:<10} {info['phone']:<15} {info['email']:<20}")
    
    print("=" * 50)
    print(f"总计: {len(contacts)} 位联系人\n")

def main():
    """主程序"""
    while True:
        print("\n=== 通讯录管理系统 ===")
        print("1. 添加联系人")
        print("2. 删除联系人")
        print("3. 查找联系人")
        print("4. 显示所有联系人")
        print("5. 退出")
        
        choice = input("\n请选择操作(1-5): ")
        
        if choice == '1':
            add_contact()
        elif choice == '2':
            delete_contact()
        elif choice == '3':
            search_contact()
        elif choice == '4':
            show_all()
        elif choice == '5':
            print("再见!")
            break
        else:
            print("无效的选择!")

if __name__ == '__main__':
    main()
```

---

## 3.9 课后练习

### 练习1: 学生成绩管理

创建程序管理学生成绩:
- 使用字典存储学生信息`{"name": [math, english, python]}`
- 计算平均分
- 找出最高分学生

### 练习2: 列表去重

编写函数去除列表中的重复元素,保持原顺序。

### 练习3: 词频统计

统计文本中每个单词出现的次数,输出前10个高频词。

---

## 3.10 本章小结

### 核心数据类型

| 类型 | 可变性 | 示例 | 用途 |
|------|--------|------|------|
| **int** | 不可变 | `123` | 整数 |
| **float** | 不可变 | `3.14` | 浮点数 |
| **str** | 不可变 | `"hello"` | 文本 |
| **list** | 可变 | `[1,2,3]` | 有序可变集合 |
| **tuple** | 不可变 | `(1,2,3)` | 有序不可变集合 |
| **dict** | 可变 | `{"a":1}` | 键值对 |
| **set** | 可变 | `{1,2,3}` | 无序不重复集合 |

### 下一章预告

**第4章 - 控制流程**,将学习:
- 🔀 条件语句(if/elif/else)
- 🔄 循环语句(for/while)
- ⏭️ break/continue
- 📋 列表推导式

---

[← 上一章](./2-Python基础语法.md) | [返回目录](./README.md) | [下一章: 控制流程 →](./4-控制流程.md)
