# 第2章：Python基础语法

> **学习时长**: 4-5小时  
> **难度**: ⭐⭐  
> **前置知识**: 第1章 - Python环境安装与配置

## 本章目标

学完本章后,你将能够:

- ✅ 掌握Python代码规范(PEP 8)
- ✅ 正确使用注释和文档字符串
- ✅ 理解Python的缩进机制
- ✅ 使用标识符和关键字
- ✅ 进行基本的输入输出操作
- ✅ 使用各种运算符
- ✅ 编写规范的Python程序

---

## 2.1 Python代码规范 (PEP 8)

### 2.1.1 什么是PEP 8?

**PEP 8**是Python官方的代码风格指南,全称"Python Enhancement Proposal 8"。

**为什么要遵循PEP 8?**
- ✅ 提高代码可读性
- ✅ 便于团队协作
- ✅ 符合Python社区标准
- ✅ 减少代码审查时的争议

### 2.1.2 核心规范

**1. 缩进 (Indentation)**

```python
# ✅ 正确 - 使用4个空格
def my_function():
    if True:
        print("Hello")

# ❌ 错误 - 使用Tab或2个空格
def my_function():
  if True:
    print("Hello")
```

**2. 每行最大长度**

```python
# ✅ 正确 - 每行不超过79个字符
result = some_function(argument1, argument2,
                       argument3, argument4)

# ✅ 使用括号进行隐式换行
my_list = [
    1, 2, 3,
    4, 5, 6
]
```

**3. 空行**

```python
# ✅ 正确使用空行
class MyClass:
    """类定义"""
    
    def method1(self):
        """方法1"""
        pass
    
    def method2(self):
        """方法2"""
        pass


def function1():
    """顶级函数之间用2个空行"""
    pass


def function2():
    """顶级函数之间用2个空行"""
    pass
```

**4. 命名规范**

| 类型 | 命名风格 | 示例 |
|------|---------|------|
| **模块名** | 小写字母,下划线 | `my_module.py` |
| **类名** | 大驼峰(PascalCase) | `MyClass` |
| **函数名** | 小写字母,下划线 | `my_function()` |
| **变量名** | 小写字母,下划线 | `my_variable` |
| **常量名** | 大写字母,下划线 | `MAX_SIZE` |
| **私有变量** | 前置单下划线 | `_private_var` |

**示例**:

```python
# ✅ 正确的命名
MAX_CONNECTIONS = 100  # 常量
user_name = "Alice"    # 变量

class UserAccount:     # 类名
    def __init__(self):
        self._balance = 0  # 私有属性
    
    def get_balance(self):  # 方法名
        return self._balance

# ❌ 错误的命名
maxConnections = 100   # 常量应全大写
UserName = "Alice"     # 变量不应大驼峰
class user_account:    # 类名应大驼峰
    pass
```

**5. 导入顺序**

```python
# ✅ 正确的导入顺序
# 1. 标准库
import os
import sys

# 2. 第三方库
import numpy as np
import pandas as pd

# 3. 本地模块
from my_module import MyClass
```

**6. 空格使用**

```python
# ✅ 正确使用空格
x = 1                    # 赋值运算符两边有空格
y = x + 2                # 算术运算符两边有空格
my_list = [1, 2, 3]      # 逗号后面有空格
result = function(1, 2)  # 函数参数逗号后有空格

# ❌ 错误使用空格
x=1                      # 缺少空格
my_list = [1,2,3]        # 逗号后缺少空格
result = function (1, 2) # 函数名和括号间不应有空格
```

---

## 2.2 注释与文档字符串

### 2.2.1 单行注释

使用`#`符号:

```python
# 这是单行注释
x = 5  # 也可以在代码后面添加注释

# 注释应该说明"为什么",而不是"是什么"
# ✅ 好的注释
total = price * 1.08  # 加上8%的税

# ❌ 不好的注释
total = price * 1.08  # 将price乘以1.08
```

### 2.2.2 多行注释

使用连续的`#`或三引号:

```python
# 方式1: 连续的#
# 这是多行注释的第一行
# 这是多行注释的第二行
# 这是多行注释的第三行

# 方式2: 三引号(实际是字符串,但常用作注释)
"""
这是多行注释
可以跨越多行
通常用于文档字符串
"""
```

### 2.2.3 文档字符串 (Docstring)

**作用**: 为函数、类、模块提供文档说明

```python
def calculate_area(radius):
    """
    计算圆的面积
    
    参数:
        radius (float): 圆的半径
    
    返回:
        float: 圆的面积
    
    示例:
        >>> calculate_area(5)
        78.53975
    """
    import math
    return math.pi * radius ** 2


class Rectangle:
    """
    矩形类
    
    属性:
        width (float): 矩形的宽度
        height (float): 矩形的高度
    """
    
    def __init__(self, width, height):
        """
        初始化矩形
        
        参数:
            width (float): 宽度
            height (float): 高度
        """
        self.width = width
        self.height = height
    
    def area(self):
        """计算矩形面积"""
        return self.width * self.height
```

**查看文档字符串**:

```python
# 使用help()函数
help(calculate_area)

# 或访问__doc__属性
print(calculate_area.__doc__)
```

---

## 2.3 缩进与代码块

### 2.3.1 Python的缩进机制

**重要**: Python使用缩进来表示代码块,不使用大括号`{}`

```python
# ✅ 正确的缩进
if True:
    print("这是代码块内部")
    print("仍然在代码块内部")
print("这是代码块外部")

# ❌ 缩进错误
if True:
    print("正确")
  print("错误!缩进不一致")  # IndentationError
```

### 2.3.2 多层嵌套

```python
# 多层缩进
for i in range(3):              # 第一层
    print(f"外层循环: {i}")
    for j in range(2):          # 第二层
        print(f"  内层循环: {j}")
        if j == 1:              # 第三层
            print("    条件满足")

# 输出:
# 外层循环: 0
#   内层循环: 0
#   内层循环: 1
#     条件满足
# 外层循环: 1
# ...
```

### 2.3.3 推荐的缩进工具

**VS Code设置**:
```json
{
    "editor.tabSize": 4,
    "editor.insertSpaces": true,
    "python.linting.pylintEnabled": true
}
```

---

## 2.4 标识符与关键字

### 2.4.1 标识符规则

**标识符**是用于命名变量、函数、类等的名称。

**命名规则**:
1. 只能包含字母、数字、下划线
2. 不能以数字开头
3. 区分大小写
4. 不能使用关键字

```python
# ✅ 合法的标识符
name = "Alice"
user_name = "Bob"
_private = 10
my_var2 = 20

# ❌ 非法的标识符
2name = "Error"      # 不能以数字开头
user-name = "Error"  # 不能包含连字符
for = 5              # 不能使用关键字
```

### 2.4.2 Python关键字

**查看所有关键字**:

```python
import keyword
print(keyword.kwlist)
```

**Python 3.11关键字列表**:

```python
['False', 'None', 'True', 'and', 'as', 'assert', 'async', 
 'await', 'break', 'class', 'continue', 'def', 'del', 
 'elif', 'else', 'except', 'finally', 'for', 'from', 
 'global', 'if', 'import', 'in', 'is', 'lambda', 'nonlocal', 
 'not', 'or', 'pass', 'raise', 'return', 'try', 'while', 
 'with', 'yield']
```

**检查是否是关键字**:

```python
import keyword

print(keyword.iskeyword('if'))    # True
print(keyword.iskeyword('name'))  # False
```

---

## 2.5 输入与输出

### 2.5.1 输出 - print()函数

**基本用法**:

```python
# 基本输出
print("Hello, World!")

# 输出多个值
print("姓名:", "Alice", "年龄:", 25)
# 输出: 姓名: Alice 年龄: 25

# 自定义分隔符
print("A", "B", "C", sep="-")
# 输出: A-B-C

# 自定义结束符
print("第一行", end=" ")
print("第二行")
# 输出: 第一行 第二行
```

**格式化输出**:

**方式1: 旧式格式化 (%)**

```python
name = "Alice"
age = 25
print("我是%s,今年%d岁" % (name, age))
# 输出: 我是Alice,今年25岁

# 格式化数字
pi = 3.1415926
print("π的值约为%.2f" % pi)
# 输出: π的值约为3.14
```

**方式2: str.format()**

```python
name = "Bob"
age = 30
print("我是{},今年{}岁".format(name, age))
# 输出: 我是Bob,今年30岁

# 指定位置
print("{1}比{0}大".format(5, 10))
# 输出: 10比5大

# 指定参数名
print("姓名:{name},年龄:{age}".format(name="Charlie", age=35))
# 输出: 姓名:Charlie,年龄:35

# 数字格式化
print("π ≈ {:.2f}".format(3.1415926))
# 输出: π ≈ 3.14
```

**方式3: f-string (推荐,Python 3.6+)**

```python
name = "David"
age = 40
print(f"我是{name},今年{age}岁")
# 输出: 我是David,今年40岁

# 表达式
x = 10
y = 20
print(f"{x} + {y} = {x + y}")
# 输出: 10 + 20 = 30

# 格式化
pi = 3.1415926
print(f"π ≈ {pi:.2f}")
# 输出: π ≈ 3.14

# 对齐
print(f"{'左对齐':<10}|")
print(f"{'右对齐':>10}|")
print(f"{'居中':^10}|")
# 输出:
# 左对齐      |
#       右对齐|
#    居中     |
```

### 2.5.2 输入 - input()函数

```python
# 基本输入
name = input("请输入你的名字: ")
print(f"你好, {name}!")

# input()返回的总是字符串
age = input("请输入年龄: ")
print(type(age))  # <class 'str'>

# 转换为数字
age = int(input("请输入年龄: "))
print(type(age))  # <class 'int'>

# 输入多个值
x, y = input("请输入两个数字(用空格分隔): ").split()
x = int(x)
y = int(y)
print(f"{x} + {y} = {x + y}")
```

**实战示例 - 用户信息录入**:

```python
print("=== 用户信息录入 ===")

name = input("姓名: ")
age = int(input("年龄: "))
height = float(input("身高(cm): "))
is_student = input("是否是学生(yes/no): ").lower() == 'yes'

print("\n=== 录入完成 ===")
print(f"姓名: {name}")
print(f"年龄: {age}岁")
print(f"身高: {height}cm")
print(f"学生: {'是' if is_student else '否'}")
```

---

## 2.6 变量与赋值

### 2.6.1 变量的创建

```python
# 直接赋值创建变量
x = 10
name = "Alice"
is_valid = True

# 多重赋值
a = b = c = 100

# 同时赋值多个变量
x, y, z = 1, 2, 3

# 交换变量
a, b = 10, 20
a, b = b, a  # 交换
print(a, b)  # 输出: 20 10
```

### 2.6.2 变量的命名建议

```python
# ✅ 好的命名 - 有意义
user_name = "Alice"
total_price = 100
is_active = True

# ❌ 差的命名 - 无意义
a = "Alice"
x = 100
flag = True

# ✅ 布尔变量使用is/has开头
is_valid = True
has_permission = False

# ✅ 复数形式表示集合
students = ["Alice", "Bob", "Charlie"]
scores = [90, 85, 95]
```

### 2.6.3 变量的类型

Python是**动态类型**语言:

```python
# 变量可以改变类型
x = 10        # x是整数
print(type(x))  # <class 'int'>

x = "Hello"   # x现在是字符串
print(type(x))  # <class 'str'>

x = [1, 2, 3] # x现在是列表
print(type(x))  # <class 'list'>
```

**类型检查**:

```python
x = 10

# 使用type()
print(type(x))  # <class 'int'>

# 使用isinstance()
print(isinstance(x, int))    # True
print(isinstance(x, str))    # False
print(isinstance(x, (int, float)))  # True (检查多个类型)
```

---

## 2.7 运算符

### 2.7.1 算术运算符

```python
a = 10
b = 3

print(a + b)   # 13  加法
print(a - b)   # 7   减法
print(a * b)   # 30  乘法
print(a / b)   # 3.333... 除法(浮点)
print(a // b)  # 3   整除
print(a % b)   # 1   取余
print(a ** b)  # 1000 幂运算(10的3次方)
```

### 2.7.2 比较运算符

```python
x = 10
y = 20

print(x == y)  # False 等于
print(x != y)  # True  不等于
print(x > y)   # False 大于
print(x < y)   # True  小于
print(x >= y)  # False 大于等于
print(x <= y)  # True  小于等于
```

### 2.7.3 赋值运算符

```python
x = 10

x += 5   # 相当于 x = x + 5
print(x) # 15

x -= 3   # 相当于 x = x - 3
print(x) # 12

x *= 2   # 相当于 x = x * 2
print(x) # 24

x /= 4   # 相当于 x = x / 4
print(x) # 6.0

x //= 2  # 相当于 x = x // 2
print(x) # 3.0

x %= 2   # 相当于 x = x % 2
print(x) # 1.0

x **= 3  # 相当于 x = x ** 3
print(x) # 1.0
```

### 2.7.4 逻辑运算符

```python
# and - 逻辑与
print(True and True)    # True
print(True and False)   # False
print(False and False)  # False

# or - 逻辑或
print(True or True)     # True
print(True or False)    # True
print(False or False)   # False

# not - 逻辑非
print(not True)         # False
print(not False)        # True

# 实际应用
age = 25
has_license = True

if age >= 18 and has_license:
    print("可以开车")
else:
    print("不能开车")
```

### 2.7.5 成员运算符

```python
# in - 成员存在
my_list = [1, 2, 3, 4, 5]
print(3 in my_list)     # True
print(10 in my_list)    # False

# not in - 成员不存在
print(10 not in my_list)  # True

# 字符串中使用
text = "Hello, World!"
print("Hello" in text)    # True
print("Python" in text)   # False
```

### 2.7.6 身份运算符

```python
# is - 判断是否是同一个对象
a = [1, 2, 3]
b = a
c = [1, 2, 3]

print(a is b)   # True  (指向同一对象)
print(a is c)   # False (不同对象,即使内容相同)
print(a == c)   # True  (内容相同)

# is not
print(a is not c)  # True

# None的判断
x = None
print(x is None)      # True  (推荐)
print(x == None)      # True  (不推荐)
```

### 2.7.7 位运算符

```python
a = 60  # 二进制: 0011 1100
b = 13  # 二进制: 0000 1101

print(a & b)   # 12  按位与
print(a | b)   # 61  按位或
print(a ^ b)   # 49  按位异或
print(~a)      # -61 按位取反
print(a << 2)  # 240 左移
print(a >> 2)  # 15  右移
```

### 2.7.8 运算符优先级

从高到低:

1. `**` (幂运算)
2. `~`, `+`, `-` (一元运算)
3. `*`, `/`, `//`, `%`
4. `+`, `-` (二元运算)
5. `<<`, `>>`
6. `&`
7. `^`, `|`
8. `==`, `!=`, `>`, `<`, `>=`, `<=`
9. `is`, `is not`, `in`, `not in`
10. `not`
11. `and`
12. `or`

```python
# 使用括号改变优先级
result = 2 + 3 * 4
print(result)  # 14

result = (2 + 3) * 4
print(result)  # 20

# 复杂表达式
result = 10 + 5 * 2 ** 2
print(result)  # 10 + 5 * 4 = 10 + 20 = 30
```

---

## 2.8 实验:综合练习

### 实验1: BMI计算器

```python
"""
BMI计算器
BMI = 体重(kg) / 身高(m)²
"""

print("=" * 40)
print("        BMI计算器")
print("=" * 40)

# 输入数据
name = input("\n请输入姓名: ")
weight = float(input("请输入体重(kg): "))
height = float(input("请输入身高(cm): "))

# 计算BMI
height_m = height / 100  # 转换为米
bmi = weight / (height_m ** 2)

# 判断健康状况
if bmi < 18.5:
    status = "偏瘦"
    advice = "建议增加营养摄入"
elif bmi < 24:
    status = "正常"
    advice = "继续保持!"
elif bmi < 28:
    status = "偏胖"
    advice = "建议适量运动"
else:
    status = "肥胖"
    advice = "建议加强锻炼,控制饮食"

# 输出结果
print("\n" + "=" * 40)
print(f"姓名: {name}")
print(f"身高: {height}cm")
print(f"体重: {weight}kg")
print(f"BMI: {bmi:.2f}")
print(f"状态: {status}")
print(f"建议: {advice}")
print("=" * 40)
```

### 实验2: 温度转换器

```python
"""
温度转换器
摄氏度 ↔ 华氏度
公式: F = C × 9/5 + 32
     C = (F - 32) × 5/9
"""

print("=" * 40)
print("      温度转换器")
print("=" * 40)
print("1. 摄氏度 → 华氏度")
print("2. 华氏度 → 摄氏度")

choice = input("\n请选择转换方向(1或2): ")

if choice == '1':
    celsius = float(input("请输入摄氏度: "))
    fahrenheit = celsius * 9/5 + 32
    print(f"\n{celsius}°C = {fahrenheit:.2f}°F")
elif choice == '2':
    fahrenheit = float(input("请输入华氏度: "))
    celsius = (fahrenheit - 32) * 5/9
    print(f"\n{fahrenheit}°F = {celsius:.2f}°C")
else:
    print("\n无效的选择!")
```

---

## 2.9 课后练习

### 练习1: 个人信息卡片

编写程序输入个人信息并格式化输出:

```
╔════════════════════════╗
║      个人信息卡片      ║
╠════════════════════════╣
║ 姓名: 张三             ║
║ 年龄: 25岁             ║
║ 城市: 北京             ║
║ 职业: 程序员           ║
╚════════════════════════╝
```

### 练习2: 简单计算器Plus

扩展calculator.py,增加:
1. 输入验证(检查除数是否为0)
2. 支持多次计算
3. 计算历史记录

### 练习3: 成绩等级判定

输入分数(0-100),输出等级:
- 90-100: A(优秀)
- 80-89: B(良好)
- 70-79: C(中等)
- 60-69: D(及格)
- 0-59: E(不及格)

---

## 2.10 本章小结

### 核心知识点

✅ **PEP 8**: Python官方代码规范,4空格缩进

✅ **注释**: 单行`#`,多行`"""`,文档字符串

✅ **缩进**: Python用缩进表示代码块

✅ **标识符**: 字母/数字/下划线,不能以数字开头

✅ **输入输出**: `input()`和`print()`,f-string格式化

✅ **变量**: 动态类型,直接赋值创建

✅ **运算符**: 算术/比较/逻辑/赋值/成员/身份

### 代码规范要点

```python
# ✅ 规范的Python代码示例
MAX_SIZE = 100  # 常量全大写

class UserAccount:  # 类名大驼峰
    """用户账户类"""
    
    def __init__(self, username):
        self.username = username  # 小写下划线
    
    def get_info(self):  # 方法名小写下划线
        """获取用户信息"""
        return f"用户名: {self.username}"
```

### 下一章预告

**第3章 - 数据类型与变量**,将深入学习:
- 🔢 数字类型(int/float/complex)
- 📝 字符串操作
- 📋 列表/元组/字典/集合
- 🔄 类型转换

---

**🎉 恭喜!** 你已经掌握了Python的基础语法!

[← 上一章](./1-Python环境安装与配置.md) | [返回目录](./README.md) | [下一章: 数据类型与变量 →](./3-数据类型与变量.md)
