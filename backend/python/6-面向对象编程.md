# 第6章：面向对象编程

> **学习时长**: 8-10小时  
> **难度**: ⭐⭐⭐⭐⭐  
> **前置知识**: 第1-5章

## 本章目标

学完本章后,你将能够:

- ✅ 理解面向对象编程(OOP)的核心概念
- ✅ 创建和使用类与对象
- ✅ 掌握封装、继承、多态三大特性
- ✅ 使用魔法方法(Magic Methods)
- ✅ 理解类方法、静态方法、实例方法
- ✅ 应用面向对象设计模式

---

## 6.1 类与对象基础

### 6.1.1 什么是面向对象?

**面向对象编程(OOP)** 是一种编程范式,将现实世界抽象为对象。

**核心概念**:
- **类(Class)**: 对象的模板/蓝图
- **对象(Object)**: 类的实例
- **属性(Attribute)**: 对象的特征/数据
- **方法(Method)**: 对象的行为/函数

### 6.1.2 定义类

```python
# 基本语法
class ClassName:
    """类的文档字符串"""
    
    def __init__(self, parameters):
        """构造方法"""
        self.attribute = value
    
    def method(self):
        """实例方法"""
        pass

# 简单示例
class Dog:
    """狗类"""
    
    def __init__(self, name, age):
        """初始化"""
        self.name = name
        self.age = age
    
    def bark(self):
        """叫"""
        print(f"{self.name}在叫: 汪汪!")
    
    def info(self):
        """信息"""
        print(f"我是{self.name},今年{self.age}岁")
```

### 6.1.3 创建对象

```python
# 创建Dog类的实例
my_dog = Dog("旺财", 3)
your_dog = Dog("小白", 2)

# 访问属性
print(my_dog.name)   # 旺财
print(your_dog.age)  # 2

# 调用方法
my_dog.bark()   # 旺财在叫: 汪汪!
my_dog.info()   # 我是旺财,今年3岁

# 修改属性
my_dog.age = 4
print(my_dog.age)  # 4
```

### 6.1.4 self参数

```python
class Person:
    def __init__(self, name):
        self.name = name  # self指向当前实例
    
    def greet(self):
        # self用于访问实例属性
        print(f"你好,我是{self.name}")

# 调用时不需要传递self
person = Person("Alice")
person.greet()  # 你好,我是Alice

# 等价于:
# Person.greet(person)
```

---

## 6.2 封装

### 6.2.1 属性访问控制

```python
class BankAccount:
    def __init__(self, owner, balance=0):
        self.owner = owner       # 公开属性
        self._account_no = "123" # 受保护属性(约定)
        self.__balance = balance # 私有属性
    
    def deposit(self, amount):
        """存款"""
        if amount > 0:
            self.__balance += amount
            print(f"存入{amount}元,余额{self.__balance}元")
    
    def withdraw(self, amount):
        """取款"""
        if 0 < amount <= self.__balance:
            self.__balance -= amount
            print(f"取出{amount}元,余额{self.__balance}元")
        else:
            print("余额不足")
    
    def get_balance(self):
        """获取余额"""
        return self.__balance

# 使用
account = BankAccount("Alice", 1000)
print(account.owner)        # Alice (公开)
print(account._account_no)  # 123 (可访问但不建议)
# print(account.__balance)  # AttributeError (无法直接访问)

account.deposit(500)
print(account.get_balance())  # 1500
```

### 6.2.2 属性装饰器(@property)

```python
class Circle:
    def __init__(self, radius):
        self._radius = radius
    
    @property
    def radius(self):
        """获取半径"""
        return self._radius
    
    @radius.setter
    def radius(self, value):
        """设置半径"""
        if value > 0:
            self._radius = value
        else:
            raise ValueError("半径必须大于0")
    
    @property
    def area(self):
        """计算面积(只读)"""
        return 3.14159 * self._radius ** 2
    
    @property
    def circumference(self):
        """计算周长(只读)"""
        return 2 * 3.14159 * self._radius

# 使用
circle = Circle(5)
print(circle.radius)      # 5
print(circle.area)        # 78.53975
print(circle.circumference)  # 31.4159

circle.radius = 10        # 使用setter
print(circle.area)        # 314.159

# circle.area = 100       # AttributeError (只读)
```

---

## 6.3 继承

### 6.3.1 基本继承

```python
# 父类(基类)
class Animal:
    def __init__(self, name):
        self.name = name
    
    def speak(self):
        print(f"{self.name}发出声音")
    
    def move(self):
        print(f"{self.name}在移动")

# 子类(派生类)
class Dog(Animal):
    def speak(self):
        """重写父类方法"""
        print(f"{self.name}汪汪叫")
    
    def fetch(self):
        """子类特有方法"""
        print(f"{self.name}去捡球")

class Cat(Animal):
    def speak(self):
        print(f"{self.name}喵喵叫")
    
    def climb(self):
        print(f"{self.name}爬树")

# 使用
dog = Dog("旺财")
dog.speak()  # 旺财汪汪叫 (调用重写的方法)
dog.move()   # 旺财在移动 (继承的方法)
dog.fetch()  # 旺财去捡球 (子类方法)

cat = Cat("小白")
cat.speak()  # 小白喵喵叫
cat.climb()  # 小白爬树
```

### 6.3.2 super()函数

```python
class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    
    def introduce(self):
        print(f"我叫{self.name},今年{self.age}岁")

class Student(Person):
    def __init__(self, name, age, student_id):
        # 调用父类构造方法
        super().__init__(name, age)
        self.student_id = student_id
    
    def introduce(self):
        # 调用父类方法
        super().introduce()
        print(f"学号: {self.student_id}")

# 使用
student = Student("Alice", 20, "S001")
student.introduce()
# 输出:
# 我叫Alice,今年20岁
# 学号: S001
```

### 6.3.3 多重继承

```python
class Flyable:
    def fly(self):
        print("我会飞")

class Swimmable:
    def swim(self):
        print("我会游泳")

class Duck(Flyable, Swimmable):
    def quack(self):
        print("嘎嘎嘎")

# 使用
duck = Duck()
duck.fly()   # 我会飞
duck.swim()  # 我会游泳
duck.quack() # 嘎嘎嘎

# 查看继承顺序(MRO)
print(Duck.__mro__)
```

---

## 6.4 多态

### 6.4.1 多态性

```python
class Shape:
    def area(self):
        pass
    
    def perimeter(self):
        pass

class Rectangle(Shape):
    def __init__(self, width, height):
        self.width = width
        self.height = height
    
    def area(self):
        return self.width * self.height
    
    def perimeter(self):
        return 2 * (self.width + self.height)

class Circle(Shape):
    def __init__(self, radius):
        self.radius = radius
    
    def area(self):
        return 3.14159 * self.radius ** 2
    
    def perimeter(self):
        return 2 * 3.14159 * self.radius

# 多态:不同对象调用同名方法表现不同
def print_shape_info(shape):
    """接受任何Shape子类"""
    print(f"面积: {shape.area():.2f}")
    print(f"周长: {shape.perimeter():.2f}")

rect = Rectangle(10, 5)
circle = Circle(5)

print_shape_info(rect)
# 面积: 50.00
# 周长: 30.00

print_shape_info(circle)
# 面积: 78.54
# 周长: 31.42
```

---

## 6.5 魔法方法

### 6.5.1 构造与析构

```python
class MyClass:
    def __init__(self, value):
        """构造方法"""
        print(f"创建对象,值={value}")
        self.value = value
    
    def __del__(self):
        """析构方法"""
        print(f"销毁对象,值={self.value}")

obj = MyClass(10)
# 创建对象,值=10

del obj
# 销毁对象,值=10
```

### 6.5.2 字符串表示

```python
class Point:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    
    def __str__(self):
        """print()使用"""
        return f"Point({self.x}, {self.y})"
    
    def __repr__(self):
        """交互式环境使用"""
        return f"Point(x={self.x}, y={self.y})"

p = Point(3, 4)
print(p)        # Point(3, 4)
print(repr(p))  # Point(x=3, y=4)
```

### 6.5.3 运算符重载

```python
class Vector:
    def __init__(self, x, y):
        self.x = x
        self.y = y
    
    def __add__(self, other):
        """+ 运算符"""
        return Vector(self.x + other.x, self.y + other.y)
    
    def __sub__(self, other):
        """- 运算符"""
        return Vector(self.x - other.x, self.y - other.y)
    
    def __mul__(self, scalar):
        """* 运算符(标量乘法)"""
        return Vector(self.x * scalar, self.y * scalar)
    
    def __eq__(self, other):
        """== 运算符"""
        return self.x == other.x and self.y == other.y
    
    def __str__(self):
        return f"Vector({self.x}, {self.y})"

# 使用
v1 = Vector(1, 2)
v2 = Vector(3, 4)

v3 = v1 + v2
print(v3)  # Vector(4, 6)

v4 = v2 - v1
print(v4)  # Vector(2, 2)

v5 = v1 * 3
print(v5)  # Vector(3, 6)

print(v1 == v2)  # False
```

### 6.5.4 容器类型方法

```python
class MyList:
    def __init__(self):
        self._items = []
    
    def __len__(self):
        """len()函数"""
        return len(self._items)
    
    def __getitem__(self, index):
        """索引访问 obj[index]"""
        return self._items[index]
    
    def __setitem__(self, index, value):
        """索引赋值 obj[index] = value"""
        self._items[index] = value
    
    def __contains__(self, item):
        """in 运算符"""
        return item in self._items
    
    def append(self, item):
        self._items.append(item)
    
    def __iter__(self):
        """迭代"""
        return iter(self._items)

# 使用
my_list = MyList()
my_list.append(1)
my_list.append(2)
my_list.append(3)

print(len(my_list))     # 3
print(my_list[0])       # 1
my_list[0] = 10
print(2 in my_list)     # True

for item in my_list:
    print(item, end=" ")  # 10 2 3
```

### 6.5.5 上下文管理器

```python
class FileManager:
    def __init__(self, filename, mode):
        self.filename = filename
        self.mode = mode
        self.file = None
    
    def __enter__(self):
        """进入with块"""
        self.file = open(self.filename, self.mode)
        return self.file
    
    def __exit__(self, exc_type, exc_val, exc_tb):
        """退出with块"""
        if self.file:
            self.file.close()

# 使用
with FileManager('test.txt', 'w') as f:
    f.write('Hello, World!')
# 自动关闭文件
```

---

## 6.6 类方法与静态方法

### 6.6.1 实例方法

```python
class MyClass:
    def instance_method(self):
        """实例方法,需要self"""
        print(f"实例方法: {self}")
```

### 6.6.2 类方法(@classmethod)

```python
class Person:
    count = 0  # 类属性
    
    def __init__(self, name):
        self.name = name
        Person.count += 1
    
    @classmethod
    def get_count(cls):
        """类方法,接收cls"""
        return cls.count
    
    @classmethod
    def from_birth_year(cls, name, birth_year):
        """工厂方法"""
        age = 2024 - birth_year
        return cls(name)

# 使用
p1 = Person("Alice")
p2 = Person("Bob")

print(Person.get_count())  # 2

p3 = Person.from_birth_year("Charlie", 1990)
```

### 6.6.3 静态方法(@staticmethod)

```python
class Math:
    @staticmethod
    def add(a, b):
        """静态方法,不需要self或cls"""
        return a + b
    
    @staticmethod
    def is_even(n):
        return n % 2 == 0

# 使用(不需要实例)
print(Math.add(3, 5))      # 8
print(Math.is_even(10))    # True
```

---

## 6.7 抽象类与接口

### 6.7.1 抽象基类

```python
from abc import ABC, abstractmethod

class Animal(ABC):
    """抽象类"""
    
    @abstractmethod
    def speak(self):
        """抽象方法,子类必须实现"""
        pass
    
    @abstractmethod
    def move(self):
        pass
    
    def sleep(self):
        """普通方法"""
        print("睡觉中...")

class Dog(Animal):
    def speak(self):
        print("汪汪!")
    
    def move(self):
        print("跑步")

# animal = Animal()  # TypeError: 无法实例化抽象类

dog = Dog()
dog.speak()  # 汪汪!
dog.move()   # 跑步
dog.sleep()  # 睡觉中...
```

---

## 6.8 实验:图书管理系统

```python
"""
图书管理系统
功能:
- 书籍管理(添加、删除、查询)
- 用户管理
- 借阅/归还
"""

from datetime import datetime, timedelta

class Book:
    """书籍类"""
    def __init__(self, isbn, title, author, total_copies):
        self.isbn = isbn
        self.title = title
        self.author = author
        self.total_copies = total_copies
        self.available_copies = total_copies
    
    def __str__(self):
        return f"{self.title} - {self.author} (ISBN: {self.isbn})"
    
    def is_available(self):
        return self.available_copies > 0

class User:
    """用户类"""
    def __init__(self, user_id, name):
        self.user_id = user_id
        self.name = name
        self.borrowed_books = []
    
    def __str__(self):
        return f"用户: {self.name} (ID: {self.user_id})"

class BorrowRecord:
    """借阅记录类"""
    def __init__(self, user, book):
        self.user = user
        self.book = book
        self.borrow_date = datetime.now()
        self.due_date = self.borrow_date + timedelta(days=30)
        self.return_date = None
    
    def is_overdue(self):
        """是否逾期"""
        if self.return_date:
            return False
        return datetime.now() > self.due_date
    
    def days_overdue(self):
        """逾期天数"""
        if not self.is_overdue():
            return 0
        return (datetime.now() - self.due_date).days

class Library:
    """图书馆类"""
    def __init__(self, name):
        self.name = name
        self.books = {}      # ISBN -> Book
        self.users = {}      # user_id -> User
        self.records = []    # 借阅记录
    
    def add_book(self, book):
        """添加书籍"""
        self.books[book.isbn] = book
        print(f"✓ 已添加书籍: {book}")
    
    def register_user(self, user):
        """注册用户"""
        self.users[user.user_id] = user
        print(f"✓ 已注册用户: {user}")
    
    def borrow_book(self, user_id, isbn):
        """借书"""
        if user_id not in self.users:
            print("✗ 用户不存在")
            return
        
        if isbn not in self.books:
            print("✗ 书籍不存在")
            return
        
        user = self.users[user_id]
        book = self.books[isbn]
        
        if not book.is_available():
            print(f"✗ {book.title} 已全部借出")
            return
        
        # 创建借阅记录
        record = BorrowRecord(user, book)
        self.records.append(record)
        user.borrowed_books.append(book)
        book.available_copies -= 1
        
        print(f"✓ {user.name} 借阅了 {book.title}")
        print(f"  应还日期: {record.due_date.strftime('%Y-%m-%d')}")
    
    def return_book(self, user_id, isbn):
        """还书"""
        user = self.users.get(user_id)
        book = self.books.get(isbn)
        
        if not user or not book:
            print("✗ 用户或书籍不存在")
            return
        
        # 查找借阅记录
        for record in self.records:
            if (record.user == user and 
                record.book == book and 
                record.return_date is None):
                
                record.return_date = datetime.now()
                user.borrowed_books.remove(book)
                book.available_copies += 1
                
                if record.is_overdue():
                    days = record.days_overdue()
                    print(f"⚠ {user.name} 归还了 {book.title} (逾期{days}天)")
                else:
                    print(f"✓ {user.name} 归还了 {book.title}")
                return
        
        print("✗ 未找到借阅记录")
    
    def list_books(self):
        """列出所有书籍"""
        print(f"\n{'='*60}")
        print(f"{self.name} - 藏书列表")
        print(f"{'='*60}")
        print(f"{'ISBN':<15} {'书名':<20} {'作者':<15} {'库存':<10}")
        print(f"{'-'*60}")
        
        for book in self.books.values():
            print(f"{book.isbn:<15} {book.title:<20} "
                  f"{book.author:<15} {book.available_copies}/{book.total_copies}")
        
        print(f"{'='*60}\n")

# 测试
if __name__ == '__main__':
    # 创建图书馆
    lib = Library("市图书馆")
    
    # 添加书籍
    lib.add_book(Book("978-1-1", "Python编程", "张三", 5))
    lib.add_book(Book("978-1-2", "算法导论", "李四", 3))
    lib.add_book(Book("978-1-3", "深度学习", "王五", 2))
    
    # 注册用户
    lib.register_user(User("U001", "Alice"))
    lib.register_user(User("U002", "Bob"))
    
    # 借书
    lib.borrow_book("U001", "978-1-1")
    lib.borrow_book("U002", "978-1-1")
    
    # 列出书籍
    lib.list_books()
    
    # 还书
    lib.return_book("U001", "978-1-1")
    
    lib.list_books()
```

---

## 6.9 课后练习

### 练习1: 银行系统

实现以下类:
- Account(账户基类)
- SavingsAccount(储蓄账户,有利息)
- CheckingAccount(支票账户,有透支限额)

### 练习2: 学生成绩系统

实现:
- Student类(姓名、学号、成绩)
- Course类(课程名、学分)
- GradeBook类(成绩册管理)

### 练习3: 扑克牌游戏

实现:
- Card类(花色、点数)
- Deck类(一副牌)
- Hand类(手牌)

---

## 6.10 本章小结

### OOP三大特性

| 特性 | 说明 | 关键字 |
|------|------|--------|
| **封装** | 隐藏内部实现 | `_`, `__`, `@property` |
| **继承** | 代码复用 | `class Child(Parent)` |
| **多态** | 同一接口不同实现 | 方法重写 |

### 重要魔法方法

```python
__init__    # 构造
__str__     # 字符串表示
__repr__    # 正式表示
__len__     # 长度
__getitem__ # 索引访问
__add__     # +运算符
__eq__      # ==运算符
__enter__   # with进入
__exit__    # with退出
```

### 方法类型

- **实例方法**: `def method(self):`
- **类方法**: `@classmethod def method(cls):`
- **静态方法**: `@staticmethod def method():`

### 下一章预告

**第7章 - 文件操作与异常处理**,将学习:
- 📁 文件读写
- 📂 目录操作
- ⚠️ 异常处理
- 🔒 上下文管理器

---

[← 上一章](./5-函数与模块.md) | [返回目录](./README.md) | [下一章: 文件操作与异常处理 →](./7-文件操作与异常处理.md)
