# 第5章：控制流程

> **学习时长**: 5-6小时  
> **难度**: ⭐⭐⭐  
> **前置知识**: 第1-4章

## 本章目标

学完本章后,你将能够:

- ✅ 使用If/Else条件语句
- ✅ 掌握Switch多分支选择
- ✅ 使用For/ForEach/While循环
- ✅ 理解Break/Continue流程控制
- ✅ 处理循环标签和嵌套循环
- ✅ 编写高效的控制流程代码

---

## 5.1 条件语句

### 5.1.1 If语句

**基本语法**:

```powershell
# 简单If
$age = 18
if ($age -ge 18) {
    Write-Host "Adult"
}

# If-Else
$score = 85
if ($score -ge 60) {
    Write-Host "Pass"
} else {
    Write-Host "Fail"
}

# If-ElseIf-Else
$grade = 75
if ($grade -ge 90) {
    Write-Host "A"
} elseif ($grade -ge 80) {
    Write-Host "B"
} elseif ($grade -ge 70) {
    Write-Host "C"
} elseif ($grade -ge 60) {
    Write-Host "D"
} else {
    Write-Host "F"
}
```

**单行If**:

```powershell
# 单行条件
if ($true) { Write-Host "True" }

# 三元运算符(PowerShell 7+)
$result = $age -ge 18 ? "Adult" : "Minor"

# 传统方式
$result = if ($age -ge 18) { "Adult" } else { "Minor" }
```

### 5.1.2 复杂条件

```powershell
# 多重条件
$age = 25
$hasLicense = $true

if (($age -ge 18) -and ($hasLicense -eq $true)) {
    Write-Host "Can drive"
}

# 使用-not或!
$isEmpty = $false
if (-not $isEmpty) {
    Write-Host "Not empty"
}

if (!$isEmpty) {
    Write-Host "Not empty"
}

# 嵌套条件
$score = 85
$attendance = 95

if ($score -ge 60) {
    if ($attendance -ge 80) {
        Write-Host "Pass with good attendance"
    } else {
        Write-Host "Pass but poor attendance"
    }
} else {
    Write-Host "Fail"
}
```

### 5.1.3 条件最佳实践

```powershell
# ❌ 不好: 深度嵌套
if ($condition1) {
    if ($condition2) {
        if ($condition3) {
            # 代码
        }
    }
}

# ✅ 好: 提前返回
if (-not $condition1) { return }
if (-not $condition2) { return }
if (-not $condition3) { return }
# 代码

# ❌ 不好: 重复代码
if ($type -eq "A") {
    Write-Host "Processing..."
    Process-TypeA
} elseif ($type -eq "B") {
    Write-Host "Processing..."
    Process-TypeB
}

# ✅ 好: 提取公共代码
Write-Host "Processing..."
if ($type -eq "A") {
    Process-TypeA
} elseif ($type -eq "B") {
    Process-TypeB
}
```

---

## 5.2 Switch语句

### 5.2.1 基本Switch

```powershell
# 基本语法
$day = "Monday"
switch ($day) {
    "Monday" { Write-Host "Start of week" }
    "Friday" { Write-Host "End of week" }
    "Saturday" { Write-Host "Weekend!" }
    "Sunday" { Write-Host "Weekend!" }
    default { Write-Host "Midweek" }
}

# 数字匹配
$number = 2
switch ($number) {
    1 { "One" }
    2 { "Two" }
    3 { "Three" }
    default { "Other" }
}

# 多个case
$fruit = "apple"
switch ($fruit) {
    "apple" { 
        Write-Host "It's an apple"
        Write-Host "Red or green"
    }
    "banana" { 
        Write-Host "It's a banana"
        Write-Host "Yellow"
    }
}
```

### 5.2.2 高级Switch

**多值匹配**:

```powershell
# 处理数组
$items = 1, 2, 3, "apple", "banana"
switch ($items) {
    1 { "Number one" }
    2 { "Number two" }
    3 { "Number three" }
    "apple" { "Fruit: apple" }
    "banana" { "Fruit: banana" }
}
# 输出每个匹配项
```

**通配符匹配**:

```powershell
# -Wildcard
$filename = "test.txt"
switch -Wildcard ($filename) {
    "*.txt" { "Text file" }
    "*.log" { "Log file" }
    "*.ps1" { "PowerShell script" }
    default { "Unknown file" }
}
```

**正则匹配**:

```powershell
# -Regex
$input = "user@example.com"
switch -Regex ($input) {
    '^\d+$' { "It's a number" }
    '^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$' { "It's an email" }
    '^https?://' { "It's a URL" }
    default { "Unknown format" }
}
```

**精确匹配**:

```powershell
# -Exact (默认)
$text = "Hello"
switch -Exact ($text) {
    "hello" { "Match (case-insensitive)" }
    "HELLO" { "Match (case-insensitive)" }
}

# -CaseSensitive
switch -CaseSensitive ($text) {
    "hello" { "No match" }
    "Hello" { "Exact match!" }
}
```

### 5.2.3 Switch高级用法

```powershell
# 多条件匹配
$value = 5
switch ($value) {
    {$_ -lt 0} { "Negative" }
    {$_ -eq 0} { "Zero" }
    {$_ -gt 0 -and $_ -le 10} { "Between 1 and 10" }
    {$_ -gt 10} { "Greater than 10" }
}

# 中断执行
$number = 2
switch ($number) {
    1 { "One" }
    2 { 
        "Two"
        break  # 停止匹配
    }
    {$_ -gt 1} { "Greater than 1" }  # 不会执行
}

# 继续匹配(不使用break)
$number = 2
switch ($number) {
    1 { "One" }
    2 { "Two" }
    {$_ -gt 1} { "Greater than 1" }  # 会执行
}
```

---

## 5.3 For循环

### 5.3.1 基本For循环

```powershell
# 标准For循环
for ($i = 0; $i -lt 5; $i++) {
    Write-Host "Iteration: $i"
}

# 倒序
for ($i = 10; $i -gt 0; $i--) {
    Write-Host $i
}

# 步长为2
for ($i = 0; $i -lt 10; $i += 2) {
    Write-Host $i  # 0, 2, 4, 6, 8
}

# 多变量
for ($i = 0, $j = 10; $i -lt 5; $i++, $j--) {
    Write-Host "i=$i, j=$j"
}
```

### 5.3.2 For循环应用

```powershell
# 遍历数组(使用索引)
$fruits = "apple", "banana", "orange"
for ($i = 0; $i -lt $fruits.Count; $i++) {
    Write-Host "$i : $($fruits[$i])"
}

# 生成序列
$squares = for ($i = 1; $i -le 10; $i++) {
    $i * $i
}
# $squares = @(1, 4, 9, 16, 25, 36, 49, 64, 81, 100)

# 嵌套循环
for ($i = 1; $i -le 3; $i++) {
    for ($j = 1; $j -le 3; $j++) {
        Write-Host "$i x $j = $($i * $j)"
    }
}
```

---

## 5.4 ForEach循环

### 5.4.1 ForEach语句

```powershell
# 基本ForEach
$colors = "Red", "Green", "Blue"
foreach ($color in $colors) {
    Write-Host $color
}

# 遍历范围
foreach ($num in 1..10) {
    Write-Host $num
}

# 遍历哈希表
$person = @{
    Name = "Alice"
    Age = 30
    City = "Beijing"
}

foreach ($key in $person.Keys) {
    Write-Host "$key : $($person[$key])"
}

# 使用GetEnumerator
foreach ($item in $person.GetEnumerator()) {
    Write-Host "$($item.Key) = $($item.Value)"
}
```

### 5.4.2 ForEach-Object (管道)

```powershell
# 基本用法
1..5 | ForEach-Object { $_ * 2 }

# 完整语法
1..5 | ForEach-Object {
    Write-Host "Processing: $_"
    $_ * 2
}

# 简写形式
1..5 | % { $_ * 2 }

# Begin/Process/End块
1..5 | ForEach-Object -Begin {
    Write-Host "Starting..."
    $sum = 0
} -Process {
    $sum += $_
} -End {
    Write-Host "Total: $sum"
}

# 访问属性
Get-Process | ForEach-Object {
    [PSCustomObject]@{
        Name = $_.Name
        CPU = $_.CPU
        Memory = $_.WS / 1MB
    }
}
```

### 5.4.3 ForEach vs ForEach-Object

```powershell
# ForEach语句 - 一次性加载所有项
$items = 1..1000000
foreach ($item in $items) {
    # 处理
}

# ForEach-Object - 流式处理
1..1000000 | ForEach-Object {
    # 处理
}

# 性能对比
# ForEach: 快,但占用内存
# ForEach-Object: 慢,但内存友好

# 使用建议
# 小集合 -> ForEach
# 大集合/管道 -> ForEach-Object
```

---

## 5.5 While和Do循环

### 5.5.1 While循环

```powershell
# 基本While
$i = 0
while ($i -lt 5) {
    Write-Host $i
    $i++
}

# 条件可能永远不满足
$count = 10
while ($count -lt 5) {
    # 不会执行
}

# 无限循环(小心!)
$continue = $true
while ($continue) {
    $input = Read-Host "Enter 'exit' to quit"
    if ($input -eq "exit") {
        $continue = $false
    }
}
```

### 5.5.2 Do-While循环

```powershell
# Do-While: 至少执行一次
$i = 0
do {
    Write-Host $i
    $i++
} while ($i -lt 5)

# 即使条件不满足,也执行一次
$count = 10
do {
    Write-Host "Executed once"
} while ($count -lt 5)
```

### 5.5.3 Do-Until循环

```powershell
# Do-Until: 直到条件为真
$i = 0
do {
    Write-Host $i
    $i++
} until ($i -ge 5)

# 用户输入验证
do {
    $age = Read-Host "Enter your age"
    $age = $age -as [int]
} until (($age -ne $null) -and ($age -gt 0) -and ($age -lt 120))

Write-Host "Valid age: $age"
```

---

## 5.6 流程控制

### 5.6.1 Break

```powershell
# 跳出循环
for ($i = 0; $i -lt 10; $i++) {
    if ($i -eq 5) {
        break  # 跳出循环
    }
    Write-Host $i
}
# 输出: 0 1 2 3 4

# 跳出Switch
$value = 2
switch ($value) {
    1 { "One" }
    2 { 
        "Two"
        break  # 停止switch
    }
    {$_ -gt 0} { "Positive" }  # 不执行
}

# 嵌套循环中的break
for ($i = 0; $i -lt 3; $i++) {
    for ($j = 0; $j -lt 3; $j++) {
        if ($j -eq 1) {
            break  # 只跳出内层循环
        }
        Write-Host "i=$i, j=$j"
    }
}
```

### 5.6.2 Continue

```powershell
# 跳过当前迭代
for ($i = 0; $i -lt 5; $i++) {
    if ($i -eq 2) {
        continue  # 跳过2
    }
    Write-Host $i
}
# 输出: 0 1 3 4

# ForEach中使用
foreach ($num in 1..10) {
    if ($num % 2 -eq 0) {
        continue  # 跳过偶数
    }
    Write-Host $num  # 只输出奇数
}

# While中使用
$i = 0
while ($i -lt 5) {
    $i++
    if ($i -eq 3) {
        continue
    }
    Write-Host $i
}
```

### 5.6.3 循环标签

```powershell
# 标签用于跳出嵌套循环
:outer for ($i = 0; $i -lt 3; $i++) {
    for ($j = 0; $j -lt 3; $j++) {
        if ($i -eq 1 -and $j -eq 1) {
            break outer  # 跳出外层循环
        }
        Write-Host "i=$i, j=$j"
    }
}

# Continue到标签
:outer foreach ($i in 1..3) {
    foreach ($j in 1..3) {
        if ($j -eq 2) {
            continue outer  # 继续外层循环
        }
        Write-Host "i=$i, j=$j"
    }
}
```

---

## 5.7 实验:控制流程综合应用

### 实验1: 菜单系统

```powershell
<#
.SYNOPSIS
    交互式菜单系统
.DESCRIPTION
    演示Switch和循环的综合应用
#>

function Show-Menu {
    param([string]$Title = "Main Menu")
    
    Clear-Host
    Write-Host "========== $Title ==========" -ForegroundColor Cyan
    Write-Host "1. Option 1 - Show Date"
    Write-Host "2. Option 2 - Show Time"
    Write-Host "3. Option 3 - Show Computer Info"
    Write-Host "4. Option 4 - List Files"
    Write-Host "Q. Quit"
    Write-Host "==============================" -ForegroundColor Cyan
}

function Start-MenuSystem {
    do {
        Show-Menu
        $selection = Read-Host "Please make a selection"
        
        switch ($selection) {
            '1' {
                Write-Host "`nCurrent Date: $(Get-Date -Format 'yyyy-MM-dd')" -ForegroundColor Green
                pause
            }
            '2' {
                Write-Host "`nCurrent Time: $(Get-Date -Format 'HH:mm:ss')" -ForegroundColor Green
                pause
            }
            '3' {
                Write-Host "`nComputer: $env:COMPUTERNAME" -ForegroundColor Green
                Write-Host "User: $env:USERNAME" -ForegroundColor Green
                pause
            }
            '4' {
                Write-Host "`nFiles in current directory:" -ForegroundColor Green
                Get-ChildItem | Select-Object Name, Length | Format-Table
                pause
            }
            'Q' {
                Write-Host "`nGoodbye!" -ForegroundColor Yellow
            }
            default {
                Write-Host "`nInvalid selection!" -ForegroundColor Red
                pause
            }
        }
    } until ($selection -eq 'Q')
}

# 运行菜单
Start-MenuSystem
```

### 实验2: 数字猜谜游戏

```powershell
<#
.SYNOPSIS
    猜数字游戏
.DESCRIPTION
    演示循环、条件判断和用户交互
#>

function Start-GuessingGame {
    $secretNumber = Get-Random -Minimum 1 -Maximum 100
    $attempts = 0
    $maxAttempts = 7
    
    Write-Host "`n=== Number Guessing Game ===" -ForegroundColor Cyan
    Write-Host "I'm thinking of a number between 1 and 100"
    Write-Host "You have $maxAttempts attempts to guess it"
    
    do {
        $attempts++
        $remaining = $maxAttempts - $attempts + 1
        
        Write-Host "`nAttempt $attempts of $maxAttempts (Remaining: $remaining)" -ForegroundColor Yellow
        $guess = Read-Host "Enter your guess"
        $guess = $guess -as [int]
        
        if ($guess -eq $null) {
            Write-Host "Please enter a valid number!" -ForegroundColor Red
            $attempts--
            continue
        }
        
        if ($guess -eq $secretNumber) {
            Write-Host "`n🎉 Congratulations! You found it in $attempts attempts!" -ForegroundColor Green
            break
        }
        elseif ($guess -lt $secretNumber) {
            Write-Host "Too low! Try higher..." -ForegroundColor Blue
        }
        else {
            Write-Host "Too high! Try lower..." -ForegroundColor Blue
        }
        
        if ($attempts -eq $maxAttempts) {
            Write-Host "`n😢 Game Over! The number was $secretNumber" -ForegroundColor Red
        }
        
    } while ($attempts -lt $maxAttempts)
}

# 开始游戏
Start-GuessingGame
```

### 实验3: 成绩统计系统

```powershell
<#
.SYNOPSIS
    学生成绩统计
.DESCRIPTION
    演示数组遍历和条件判断
#>

function Get-GradeStatistics {
    param(
        [Parameter(Mandatory)]
        [int[]]$Scores
    )
    
    Write-Host "`n=== Grade Statistics ===" -ForegroundColor Cyan
    
    # 初始化统计变量
    $total = 0
    $passed = 0
    $failed = 0
    $gradeCount = @{A=0; B=0; C=0; D=0; F=0}
    
    # 遍历所有成绩
    foreach ($score in $Scores) {
        $total += $score
        
        # 及格率统计
        if ($score -ge 60) {
            $passed++
        } else {
            $failed++
        }
        
        # 等级统计
        $grade = switch ($score) {
            {$_ -ge 90} { 'A'; break }
            {$_ -ge 80} { 'B'; break }
            {$_ -ge 70} { 'C'; break }
            {$_ -ge 60} { 'D'; break }
            default { 'F' }
        }
        $gradeCount[$grade]++
    }
    
    # 计算平均分
    $average = $total / $Scores.Count
    
    # 显示结果
    Write-Host "`nTotal Students: $($Scores.Count)"
    Write-Host "Average Score: $([Math]::Round($average, 2))"
    Write-Host "Passed: $passed ($([Math]::Round($passed/$Scores.Count*100, 2))%)"
    Write-Host "Failed: $failed ($([Math]::Round($failed/$Scores.Count*100, 2))%)"
    
    Write-Host "`nGrade Distribution:" -ForegroundColor Yellow
    foreach ($grade in $gradeCount.Keys | Sort-Object) {
        $count = $gradeCount[$grade]
        $percent = [Math]::Round($count/$Scores.Count*100, 2)
        Write-Host "  $grade`: $count ($percent%)"
    }
    
    # 最高分和最低分
    $max = ($Scores | Measure-Object -Maximum).Maximum
    $min = ($Scores | Measure-Object -Minimum).Minimum
    
    Write-Host "`nHighest Score: $max" -ForegroundColor Green
    Write-Host "Lowest Score: $min" -ForegroundColor Red
}

# 测试数据
$testScores = 95, 87, 76, 92, 88, 54, 67, 91, 83, 72, 45, 98, 85, 79, 61
Get-GradeStatistics -Scores $testScores
```

---

## 5.8 课后练习

### 练习1: 循环练习

编写脚本实现:
1. 打印九九乘法表
2. 生成斐波那契数列(前20项)
3. 找出1-100之间的所有质数

### 练习2: 条件判断

创建程序判断:
1. 三个数中的最大值
2. 判断字符串是否为回文
3. 验证密码强度(至少8位,含大小写字母和数字)

### 练习3: 综合应用

开发:
1. 简单的命令行计算器
2. 待办事项列表管理
3. 文件搜索工具

---

## 5.9 本章小结

### 核心语句

✅ **条件**: If/ElseIf/Else, Switch

✅ **循环**: For, ForEach, While, Do

✅ **控制**: Break, Continue, 标签

### 关键语法

```powershell
# 条件
if (condition) { } elseif { } else { }
switch (value) { pattern { action } }

# 循环
for ($i=0; $i -lt 10; $i++) { }
foreach ($item in $collection) { }
while (condition) { }
do { } while (condition)
do { } until (condition)

# 控制
break        # 跳出循环
continue     # 跳过当前迭代
break :label # 跳出标签循环
```

### 最佳实践

1. **避免深度嵌套** - 使用提前返回
2. **选择合适的循环** - 考虑性能和可读性
3. **使用有意义的变量名**
4. **添加注释说明复杂逻辑**

### 下一章预告

**第6章 - 函数与脚本**,将学习:
- 📦 函数定义和调用
- 🔧 参数处理
- 📝 脚本编写
- 🎯 高级函数特性

---

[← 上一章](./4-运算符与表达式.md) | [返回目录](./README.md) | [下一章: 函数与脚本 →](./6-函数与脚本.md)
