# Airflow 环境变量配置文件示例
# 复制此文件为 .env 并根据您的环境修改配置

# Airflow 核心配置
AIRFLOW_HOME=~/airflow
AIRFLOW__CORE__EXECUTOR=LocalExecutor
AIRFLOW__CORE__SQL_ALCHEMY_CONN=sqlite:////airflow/airflow.db
AIRFLOW__CORE__LOAD_EXAMPLES=true
AIRFLOW__CORE__FERNET_KEY=your-fernet-key-here

# Web 服务器配置
AIRFLOW__WEBSERVER__WEB_SERVER_HOST=0.0.0.0
AIRFLOW__WEBSERVER__WEB_SERVER_PORT=8080
AIRFLOW__WEBSERVER__SECRET_KEY=your-secret-key-here

# 调度器配置
AIRFLOW__SCHEDULER__SCHEDULER_HEARTBEAT_SEC=10

# 日志配置
AIRFLOW__LOGGING__BASE_LOG_FOLDER=/airflow/logs
AIRFLOW__LOGGING__LOG_LEVEL=INFO

# 数据库配置 (生产环境推荐使用 PostgreSQL)
# AIRFLOW__CORE__SQL_ALCHEMY_CONN=postgresql+psycopg2://airflow:airflow@postgres:5432/airflow

# Celery 配置 (如果使用 CeleryExecutor)
# AIRFLOW__CORE__EXECUTOR=CeleryExecutor
# AIRFLOW__CELERY__BROKER_URL=redis://redis:6379/0
# AIRFLOW__CELERY__RESULT_BACKEND=db+postgresql://airflow:airflow@postgres:5432/airflow

# SMTP 配置 (用于邮件通知)
# AIRFLOW__SMTP__SMTP_HOST=smtp.gmail.com
# AIRFLOW__SMTP__SMTP_PORT=587
# AIRFLOW__SMTP__SMTP_USER=your-email@gmail.com
# AIRFLOW__SMTP__SMTP_PASSWORD=your-app-password
# AIRFLOW__SMTP__SMTP_MAIL_FROM=your-email@gmail.com

# OAuth 配置 (可选)
# AIRFLOW__GOOGLE__CLIENT_ID=your-google-client-id
# AIRFLOW__GOOGLE__CLIENT_SECRET=your-google-client-secret

# Kubernetes 配置 (如果使用 KubernetesExecutor)
# AIRFLOW__CORE__EXECUTOR=KubernetesExecutor
# AIRFLOW__KUBERNETES__NAMESPACE=airflow
# AIRFLOW__KUBERNETES__WORKER_CONTAINER_REPOSITORY=apache/airflow
# AIRFLOW__KUBERNETES__WORKER_CONTAINER_TAG=latest
# AIRFLOW__KUBERNETES__WORKER_SERVICE_ACCOUNT_NAME=airflow-worker

# Elasticsearch 配置 (用于日志存储)
# AIRFLOW__ELASTICSEARCH__HOST=elasticsearch:9200
# AIRFLOW__ELASTICSEARCH__LOG_ID_TEMPLATE={{ dag_id }}-{{ task_id }}-{{ execution_date }}-{{ try_number }}

# AWS 配置 (如果使用 AWS 服务)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_DEFAULT_REGION=us-west-2

# Google Cloud 配置 (如果使用 GCP 服务)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
# GCP_PROJECT_ID=your-gcp-project-id

# Azure 配置 (如果使用 Azure 服务)
# AZURE_CLIENT_ID=your-azure-client-id
# AZURE_CLIENT_SECRET=your-azure-client-secret
# AZURE_TENANT_ID=your-azure-tenant-id
# AZURE_SUBSCRIPTION_ID=your-azure-subscription-id

# 自定义插件路径
# AIRFLOW__CORE__PLUGINS_FOLDER=/airflow/plugins

# DAG 目录配置
# AIRFLOW__CORE__DAGS_FOLDER=/airflow/dags

# 并行任务配置
# AIRFLOW__CORE__PARALLELISM=32
# AIRFLOW__CORE__DAG_CONCURRENCY=16
# AIRFLOW__CORE__MAX_ACTIVE_TASKS_PER_DAG=16

# 时区配置
# AIRFLOW__CORE__DEFAULT_TIMEZONE=UTC

# 安全配置
# AIRFLOW__WEBSERVER__RBAC=true
# AIRFLOW__WEBSERVER__EXPOSE_CONFIG=false

# 实验性功能
# AIRFLOW__WEBSERVER__ENABLE_EXPERIMENTAL_API=true

# DAG 处理配置
# AIRFLOW__SCHEDULER__DAG_DIR_LIST_INTERVAL=300
# AIRFLOW__SCHEDULER__MIN_FILE_PROCESS_INTERVAL=30
# AIRFLOW__SCHEDULER__MAX_DAGRUNS_PER_LOOP_TO_SCHEDULE=10

# 任务实例配置
# AIRFLOW__CORE__MAX_ACTIVE_RUNS_PER_DAG=1
# AIRFLOW__CORE__DAG_DISCOVERY_SAFE_MODE=true

# UI 配置
# AIRFLOW__WEBSERVER__HIDE_SENSITIVE_VAR_FIELDS=true
# AIRFLOW__WEBSERVER__EXPOSE_CONFIG=false
# AIRFLOW__WEBSERVER__AUTHENTICATE=true
# AIRFLOW__WEBSERVER__AUTH_BACKEND=airflow.api.auth.backend.default