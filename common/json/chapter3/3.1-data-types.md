# 3.1 JSON数据类型详解

## JSON数据类型概述

JSON支持六种基本数据类型：字符串（String）、数字（Number）、布尔值（Boolean）、对象（Object）、数组（Array）和null。理解这些数据类型及其在JSON中的表示方式，是有效使用JSON的基础。

## 字符串（String）类型

### 字符串基本规则

JSON中的字符串是由双引号包围的Unicode字符序列。字符串可以包含任何Unicode字符，但某些特殊字符需要转义。

```json
{
  "simpleString": "Hello, World!",
  "chineseString": "你好，世界！",
  "emptyString": "",
  "numericString": "12345",
  "booleanString": "true",
  "nullString": "null"
}
```

### 字符串中的转义字符

JSON字符串中的转义字符用于表示特殊字符或控制字符。

| 转义序列 | 表示的字符 | 说明 |
|---------|-----------|------|
| `\"` | `"` | 双引号 |
| `\\` | `\` | 反斜杠 |
| `\/` | `/` | 正斜杠（可选转义） |
| `\b` | 退格符 | 退格 |
| `\f` | 换页符 | 换页 |
| `\n` | 换行符 | 换行 |
| `\r` | 回车符 | 回车 |
| `\t` | 制表符 | 水平制表 |
| `\uXXXX` | Unicode字符 | 4位十六进制Unicode码点 |

```json
{
  "withQuotes": "他说：\"JSON很好用！\"",
  "withBackslash": "路径：C:\\Users\\Documents",
  "withNewlines": "第一行\n第二行\n第三行",
  "withTabs": "列1\t列2\t列3",
  "withUnicodeEscape": "\u4F60\u597D",  // Unicode转义表示"你好"
  "controlChars": "退格\b换页\f换行\n回车\r制表\t"
}
```

### Unicode支持

JSON原生支持Unicode字符，可以直接使用各种语言的字符，包括中文、日文、阿拉伯文、表情符号等。

```json
{
  "chinese": "中文示例",
  "japanese": "日本語の例",
  "arabic": "مثال عربي",
  "emoji": "😀🎉🚀",
  "math": "数学符号：∑∏∫",
  "currency": "货币符号：$€£¥₹"
}
```

### 字符串值的最佳实践

1. **使用语义化的字符串**：字符串内容应清晰表达其含义
2. **避免过长的字符串**：考虑将大文本分割或使用外部存储
3. **国际化考虑**：使用UTF-8编码，支持多语言
4. **避免敏感信息**：不在JSON字符串中直接存储密码等敏感信息

```json
// 良好的字符串实践
{
  "pageTitle": "用户登录",
  "errorMessage": "用户名或密码错误，请重试。",
  "welcomeMessage": "欢迎回来，{{username}}！",
  "apiEndpoint": "https://api.example.com/v1/users",
  "timestampFormat": "YYYY-MM-DD HH:mm:ss",
  "language": "zh-CN",
  "timezone": "Asia/Shanghai"
}
```

## 数字（Number）类型

### 数字的基本表示

JSON中的数字可以是整数或浮点数，支持科学计数法。

```json
{
  "positiveInteger": 42,
  "negativeInteger": -17,
  "zero": 0,
  "positiveFloat": 3.14159,
  "negativeFloat": -2.71828,
  "scientificNotation": 1.23e+4,  // 等于12300
  "negativeScientific": 5.67e-3,  // 等于0.00567
  "largeNumber": 9007199254740991,  // JavaScript中最大的安全整数
  "verySmallNumber": 0.0000001,
  "decimalOnly": .5,                // 等于0.5
  "negativeDecimal": -.75           // 等于-0.75
}
```

### 数字的限制与规则

1. **不能使用前导零**（除了0本身）
2. **不能包含空格或下划线**
3. **不支持八进制、十六进制**
4. **不支持 Infinity 或 NaN**

```json
// 正确的数字表示
{
  "validNumbers": [0, 123, -456, 3.14, -2.718, 1.23e+5]
}

// 错误的数字表示
{
  "leadingZero": 0123,    // 前导零（错误）
  "hexadecimal": 0xFF,    // 十六进制（错误）
  "octal": 0o77,          // 八进制（错误）
  "withSpaces": 1 234,    // 包含空格（错误）
  "withUnderscore": 1_234, // 包含下划线（错误）
  "infinity": Infinity,   // Infinity不是有效的JSON数字
  "notANumber": NaN       // NaN不是有效的JSON数字
}
```

### 数字的精度问题

JSON数字在解析时可能会遇到精度问题，特别是在JavaScript中：

```json
{
  "safeInteger": 9007199254740991,      // JavaScript最大安全整数
  "unsafeInteger": 9007199254740992,    // 可能丢失精度
  "highPrecisionFloat": 0.1 + 0.2,     // 浮点精度问题
  "veryLargeNumber": 12345678901234567890  // 可能超出安全范围
}
```

**解决方案**：
1. 对于需要高精度的数字，使用字符串表示
2. 对于非常大的数字，使用字符串表示
3. 在客户端进行适当的精度处理

```json
// 使用字符串表示高精度数字
{
  "largeNumberAsString": "12345678901234567890",
  "preciseDecimalAsString": "0.30000000000000004",
  "monetaryValue": "123456789.12"
}
```

### 数字类型的最佳实践

1. **使用适当的数据类型**：整数表示计数，浮点数表示测量值
2. **考虑精度问题**：对关键数据使用字符串表示数字
3. **使用科学计数法**：表示极大或极小的数字
4. **避免特殊值**：不使用Infinity或NaN

```json
// 良好的数字实践
{
  "userId": 12345,              // 用户ID - 整数
  "price": 19.99,               // 价格 - 浮点数
  "quantity": 5,                // 数量 - 整数
  "rating": 4.5,                // 评分 - 浮点数
  "atomicMass": 1.23e-10,       // 原子质量 - 科学计数法
  "planetsCount": 8,             // 行星数量 - 整数
  "largeId": "9007199254740992", // 超出安全整数范围，使用字符串
  "preciseValue": "123456789.123456789" // 高精度值，使用字符串
}
```

## 布尔值（Boolean）类型

### 布尔值的基本表示

JSON中只有两个布尔值：`true`和`false`，必须是小写。

```json
{
  "isActive": true,
  "isDeleted": false,
  "hasPermission": true,
  "isEmpty": false,
  "booleanArray": [true, false, true, false],
  "nestedBoolean": {
    "visible": true,
    "editable": false,
    "required": true
  }
}
```

### 布尔值的常见错误

```json
// 错误的布尔值表示
{
  "isActive": True,    // 首字母大写（错误）
  "isDeleted": FALSE,  // 全大写（错误）
  "hasPermission": "true",  // 使用字符串（错误）
  "isEmpty": 1,        // 使用数字（错误）
  "isCompleted": 0     // 使用数字（错误）
}

// 正确的布尔值表示
{
  "isActive": true,
  "isDeleted": false,
  "hasPermission": true,
  "isEmpty": false
}
```

### 布尔值与数字的转换

在某些场景中，可能需要布尔值和数字之间的转换：

```json
{
  "asBoolean": true,
  "asNumber": 1,          // 表示true
  "asBooleanFalse": false,
  "asNumberFalse": 0       // 表示false
}
```

**转换规则**（在某些编程语言中）：
- `true` → `1`
- `false` → `0`
- `非零数字` → `true`
- `0` → `false`
- `非空字符串` → `true`
- `空字符串` → `false`

### 布尔值的最佳实践

1. **使用语义化的布尔键名**：键名应该以"is"、"has"、"can"等开头
2. **避免混合表示法**：不要在同一个JSON中同时使用布尔值和0/1表示真假
3. **保持布尔值的一致性**：相似的逻辑使用相同的布尔表示方式
4. **明确表示状态**：使用布尔值明确表示开/关、是/否等二元状态

```json
// 良好的布尔实践
{
  "isLoggedIn": true,
  "hasAdminRights": false,
  "canEdit": true,
  "isDeleted": false,
  "notificationsEnabled": true,
  "profileCompleted": false,
  "isPublic": true,
  "requiresApproval": false,
  "isFeatured": true,
  "isActive": true
}
```

## null值

### null的基本表示

JSON中的`null`表示"无值"或"空值"，必须是小写。

```json
{
  "nullValue": null,
  "objectWithNulls": {
    "name": "张三",
    "middleName": null,  // 可选字段，用户没有提供中间名
    "nickname": null     // 可选字段，用户没有提供昵称
  },
  "arrayWithNulls": [
    "张三",
    null,               // 第二个用户信息为空
    "李四"
  ],
  "nestedNulls": {
    "level1": {
      "level2": {
        "level3": null
      }
    }
  }
}
```

### null的常见误解

```json
// 错误的null表示
{
  "uppercaseNull": NULL,    // 全大写（错误）
  "mixedCaseNull": Null,    // 首字母大写（错误）
  "quotedNull": "null",     // 使用字符串表示（错误）
  "emptyObject": {},        // 空对象不等于null
  "emptyArray": []          // 空数组不等于null
}

// 正确的null表示
{
  "correctNull": null,
  "optionalField": null,    // 表示可选字段没有值
  "missingData": null       // 表示数据缺失
}
```

### null与空值的区别

理解`null`与其他"空"值的区别非常重要：

| 值类型 | 含义 | 示例 |
|-------|------|------|
| `null` | 无值，字段存在但没有值 | `"middleName": null` |
| `""` | 空字符串，字段有值但为空 | `"description": ""` |
| `{}` | 空对象，字段有值但为空对象 | `"address": {}` |
| `[]` | 空数组，字段有值但为空数组 | `"tags": []` |

```json
{
  "user": {
    "id": 123,
    "name": "张三",
    "middleName": null,     // 用户没有中间名
    "description": "",       // 用户提供了描述但内容为空
    "avatar": {},            // 头像对象存在但没有内容
    "skills": []             // 用户没有技能
  }
}
```

### null的最佳实践

1. **明确使用null**：当字段明确表示"无值"时使用null
2. **与空值区分**：null不同于空字符串""、空对象{}和空数组[]
3. **保持一致性**：在相似情况下一致地使用null
4. **文档化约定**：明确约定null在特定上下文中的含义

```json
// 良好的null实践
{
  "user": {
    "id": 123,
    "name": "张三",
    "middleName": null,     // 用户没有中间名
    "avatar": "https://example.com/avatar.jpg",
    "lastLoginAt": null      // 用户从未登录
  },
  "product": {
    "id": 456,
    "name": "智能手机",
    "description": "",       // 描述是空字符串，不是null
    "discountPrice": null,   // 没有折扣价
    "tags": []               // 没有标签，是空数组，不是null
  }
}
```

## 实验环节：数据类型验证与转换

### 实验1：数据类型识别

创建一个包含各种数据类型的JSON文件，并编写程序验证每个字段的类型：

**文件：`data_types_test.json`**
```json
{
  "stringField": "这是一个字符串",
  "numericString": "12345",
  "emptyString": "",
  "integerField": 42,
  "floatField": 3.14159,
  "scientificNotation": 1.23e+5,
  "booleanTrue": true,
  "booleanFalse": false,
  "nullField": null,
  "emptyObject": {},
  "emptyArray": [],
  "arrayOfMixedTypes": ["string", 123, true, null, {"key": "value"}]
}
```

**验证程序**（JavaScript示例）：
```javascript
// 验证数据类型
function validateDataTypes(jsonObj) {
  const results = {};
  
  for (const key in jsonObj) {
    const value = jsonObj[key];
    let type;
    
    if (value === null) {
      type = 'null';
    } else if (Array.isArray(value)) {
      type = 'array';
    } else if (typeof value === 'object') {
      type = 'object';
    } else if (typeof value === 'string') {
      type = 'string';
    } else if (typeof value === 'number') {
      type = 'number';
    } else if (typeof value === 'boolean') {
      type = 'boolean';
    }
    
    results[key] = {
      value: value,
      type: type,
      isStringNumeric: typeof value === 'string' && !isNaN(value)
    };
  }
  
  return results;
}
```

### 实验2：数字精度问题演示

创建一个JSON文件，演示数字精度问题及其解决方案：

**文件：`precision_issues.json`**
```json
{
  "safeInteger": 9007199254740991,
  "unsafeInteger": 9007199254740992,
  "highPrecisionFloat": 0.1 + 0.2,
  "stringNumber": "9007199254740992",
  "preciseFloatAsString": "0.30000000000000004",
  "monetaryValue": "123456789.123456789"
}
```

### 实验3：布尔值与数字转换演示

创建一个JSON文件，演示布尔值与数字之间的转换：

**文件：`boolean_conversion.json`**
```json
{
  "asBoolean": true,
  "asNumber": 1,
  "asBooleanFalse": false,
  "asNumberFalse": 0,
  "truthyValues": [true, 1, "true", "non-empty"],
  "falsyValues": [false, 0, "", null]
}
```

## 小结

本节我们详细探讨了JSON的各种数据类型：

1. **字符串类型**：
   - 基本规则：双引号包围的Unicode字符序列
   - 转义字符：特殊字符的表示方式
   - Unicode支持：原生支持多语言字符
   - 最佳实践：语义化、避免过长、国际化考虑

2. **数字类型**：
   - 基本表示：整数、浮点数、科学计数法
   - 限制与规则：前导零、特殊字符限制
   - 精度问题：JavaScript中的安全整数范围
   - 最佳实践：适当类型、考虑精度、避免特殊值

3. **布尔值类型**：
   - 基本表示：true和false（小写）
   - 常见错误：大小写、字符串表示、数字表示
   - 与数字的转换：不同语言中的转换规则
   - 最佳实践：语义化键名、一致性、明确状态

4. **null值**：
   - 基本表示：null（小写）
   - 常见误解：大小写、与空值的区别
   - 与空值的区别：null vs "" vs {} vs []
   - 最佳实践：明确使用、保持一致性、文档化约定

理解这些数据类型及其规则，是创建有效、可靠的JSON文档的基础。在下一节中，我们将探讨JSON中的对象与数组结构，了解如何组织和表示复杂的数据关系。

## 代码示例

请查看`code/chapter3/data-types`目录，其中包含了：

1. `string_examples.json` - 各种字符串类型示例
2. `number_examples.json` - 数字类型示例及精度问题
3. `boolean_examples.json` - 布尔值类型示例
4. `null_examples.json` - null值示例及与空值的对比
5. `data_types_validation.js` - JavaScript数据类型验证代码
6. `data_types_validation.py` - Python数据类型验证代码
7. `precision_demo.js` - 数字精度问题演示代码
8. `boolean_conversion_demo.js` - 布尔值转换演示代码