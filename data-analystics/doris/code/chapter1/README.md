# 第1章代码示例：Apache Doris 基础入门

本章包含Apache Doris基础入门相关的代码示例，帮助读者快速上手Doris的基本操作。

## 📁 文件结构

```
chapter1/
├── README.md              # 本文件，章节代码说明
├── docker-deploy/         # Docker部署示例
│   ├── docker-compose.yml # Docker Compose部署配置
│   └── init-sql/          # 初始化SQL脚本
├── basic-operations/      # 基本操作示例
│   ├── create-table.sql   # 创建表示例
│   ├── insert-data.sql    # 插入数据示例
│   └── query-data.sql     # 查询数据示例
└── sample-data/           # 示例数据
    └── users.csv          # 用户示例数据
```

## 🚀 快速开始

### 1. 使用Docker部署Doris

```bash
# 进入docker-deploy目录
cd docker-deploy

# 启动Doris集群
docker-compose up -d

# 查看服务状态
docker-compose ps
```

### 2. 连接Doris

```bash
# 使用MySQL客户端连接
mysql -h 127.0.0.1 -P 9030 -uroot

# 或使用Docker容器内的MySQL客户端
docker exec -it doris-fe mysql -h 127.0.0.1 -P 9030 -uroot
```

### 3. 执行示例SQL

```bash
# 创建数据库和表
mysql -h 127.0.0.1 -P 9030 -uroot < basic-operations/create-table.sql

# 插入示例数据
mysql -h 127.0.0.1 -P 9030 -uroot < basic-operations/insert-data.sql

# 执行查询示例
mysql -h 127.0.0.1 -P 9030 -uroot < basic-operations/query-data.sql
```

## 📝 代码说明

### Docker部署示例

`docker-deploy/docker-compose.yml` 提供了一个完整的Doris集群部署配置，包括：
- 1个FE节点
- 1个BE节点
- 必要的端口映射
- 数据持久化配置

### 基本操作示例

`basic-operations/` 目录包含了Doris基本操作的SQL示例：

1. **创建表示例** (`create-table.sql`)
   - 创建数据库
   - 创建不同数据模型的表
   - 设置分区和分桶

2. **插入数据示例** (`insert-data.sql`)
   - 使用INSERT语句插入数据
   - 批量插入数据
   - 导入CSV文件数据

3. **查询数据示例** (`query-data.sql`)
   - 基本SELECT查询
   - 条件查询
   - 聚合查询
   - 排序和限制

## 🔧 环境要求

- Docker 20.0+
- Docker Compose 2.0+
- MySQL客户端 5.7+

## 📖 学习建议

1. 先运行Docker部署示例，搭建本地环境
2. 依次执行基本操作示例，熟悉Doris SQL语法
3. 尝试修改示例代码，观察结果变化
4. 结合章节文档，理解每个操作背后的原理

## ❓ 常见问题

### Q: Docker启动失败怎么办？
A: 检查端口是否被占用，确保Docker服务正常运行。

### Q: 连接Doris时提示连接被拒绝？
A: 确认FE节点已启动，检查端口映射是否正确。

### Q: 如何查看Doris集群状态？
A: 连接FE节点后执行 `SHOW FRONTENDS;` 和 `SHOW BACKENDS;` 命令。

---

> 返回章节文档：[第1章：Apache Doris 基础入门](../../1-基础入门.md)