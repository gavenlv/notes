## 1. Atlas Monitoring M10+
```bash
atlas processes list

atlas metrics processes <cluster id> \
--period P1D \
--granularity PT5M \
--output json \
-type connections

atlas metrics processes atlas-wppkha-shard-00-00.rohtrpj.mongodb.net:27017 --period P1D --granularity PT1M --output json --type CONNECTIONS | jq '.measurements[0].dataPoints |= .[-10:]'
```

## Atlas Config alert
```bash
# list alerting setting
atlas alerts settings list --output json
# create alert setting
 atlas alerts settings create --event JOINED_GROUP --enabled \
 --notificationIntervalMin 5 \
 --notificationType USER \
 --notificationEmailEnabled \
 --notificationUsername < username> \
 --output json --projectId <project id> 

# update alert setting
 atlas alerts settings update <alert id> \
 --event JOINED_GROUP \
 --enabled \
 --notificationIntervalMin 5 \
 --notificationType USER \
 --notificationEmailEnabled \
 --notificationUsername <username> \
 --output json \
 --projectId <project id> 
# delete alert setting
atlas alerts settings delete <alertConfigId> 

```



